fail_closed: true

claims:
  severities:
    trivial:
      citation_required: false
    non_trivial:
      citation_required: true

budgets:
  exhaustion_mode: fail  # fail | finalize_partial
  max_connector_calls: 20
  max_time_seconds: 300
  max_tokens: 200000
  max_retries_per_stage: 3
  max_evidence_items_ingested: 500

