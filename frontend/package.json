{
  "name": "researchops-studio",
  "private": true,
  "scripts": {
    "dev": "npm --prefix dashboard run dev",
    "build": "npm --prefix dashboard run build",
    "preview": "npm --prefix dashboard run preview",
    "lint:web": "npm --prefix dashboard run lint",
    "format:web": "npm --prefix dashboard run format"
  }
}
